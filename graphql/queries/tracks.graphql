query tracks {
  tracks {
    ...TrackBasicContent
    description
    numberOfViews
  }
}

fragment TrackBasicContent on Track {
  id
  title
}

query tracksBasicInfo {
  tracks {
    ...TrackBasicContent
  }
}
